function createPost() {

    // TOPICS 

    var numTopics;

    for (var i = 0; i < numTopics; i++) {
        const groupElementContainer = document.createElement('li');
        groupElementContainer.classList.add('group-element-container');
        groupElementContainer.dataset.color = iconColor;
    }
}